// 05_Core_Module.js

// Module :

    // Modules are like JavaScript libraries that can be used in a Node.js application to include a set of functions. 
    // Modules are reusable blocks of code.

    

// Module Two Type 
    // i. Core       :   Inbuild
    // ii. External  :   install by developers  and shared on the npm registry.

// Core Module : 
    // Consider modules to be the same as JavaScript libraries
    // Inbuild module in nodejs
    // No need to install
    // -- No need to import
    // No need to export
    // Core modules are built into Node.js and do not require any installation. They are written in C++ and JavaScript, optimized for performance, and provide essential functionality for creating applications.

    // In Core Module Two Type 
        // i. Global        :   require() function Not Used.
        // ii. Non Global   :   require() function Used.

    
    // require() is a built-in function with a special purpose used to load modules.
    

// package and module are both same (one thing diff name)